---
description: File map and knowledge graph for platform-ui — load so the agent can jump to the right files
alwaysApply: true
---

# platform-ui — AI file map

Start with `AGENTS.md`, then `docs/ai/KNOWLEDGE_GRAPH.md`. Do not scan the whole repo when a task is listed there.

Superadmin pages under `app/(superadmin)/`. Client `lib/api.ts`. Nav `components/layout/Sidebar.tsx`.
